import 'package:flutter/material.dart';

class MapAccess extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('MapAccess page'),
      ),
    );
  }
}
