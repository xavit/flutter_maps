import 'package:flutter/material.dart';

class GpsAccesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GpsAccesScreen page'),
      ),
    );
  }
}
