import 'package:flutter/material.dart';

class LoaginScreen extends StatelessWidget {
  const LoaginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('LoaginScreen page'),
      ),
    );
  }
}
